version: "3.9"

services:
  backend:
    build: ./backend
    container_name: ai-backend
    ports:
      - "8001:8000"
    volumes:
      - ./backend:/app
    restart: always

  frontend:
    build: ./frontend
    container_name: ai-frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: always
